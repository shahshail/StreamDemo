<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginBottom="@dimen/contextmenu_mgn_tb"
    android:layout_marginEnd="@dimen/contextmenu_mgn_lr"
    android:layout_marginStart="@dimen/contextmenu_mgn_lr"
    android:layout_marginTop="@dimen/contextmenu_mgn_tb">

    <TextView
        android:id="@+id/title_txt"
        android:layout_width="0dp"
        android:layout_height="64dp"
        android:background="@color/primary"
        android:gravity="center"
        android:maxLines="1"
        android:paddingEnd="@dimen/contextmenu_title_pad_lr"
        android:paddingStart="@dimen/contextmenu_title_pad_lr"
        android:textColor="@color/text_light"
        android:textSize="@dimen/font_large"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Title of Menu" />

    <Button
        android:id="@+id/close_btn"
        style="@style/Tio.Material.Button"
        android:layout_width="0dp"
        android:layout_height="64dp"
        android:background="@color/primary"
        android:text="@string/close"
        android:textColor="@color/text_light"
        app:layout_constraintBottom_toBottomOf="@id/title_txt"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/title_txt" />

    <FrameLayout
        android:id="@+id/content"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/title_txt" />

</android.support.constraint.ConstraintLayout>
